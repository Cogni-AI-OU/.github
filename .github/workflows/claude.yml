---
name: Claude Code

on:
  issue_comment:
    types: [created, edited]
  pull_request_review_comment:
    types: [created, edited]
  issues:
    types: [opened]
  pull_request_review:
    types: [submitted]
  workflow_call:

concurrency:
  group: claude-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  claude:
    # Security: Only allow org members/collaborators OR PR/issue authors to trigger Claude
    # author_association values: OWNER, MEMBER, COLLABORATOR are trusted
    # PR/issue authors can trigger Claude on their own PRs/issues
    # Note: For issues, only allow on creation (no assigned trigger)
    if: |
      (
        github.event.comment.author_association == 'OWNER' ||
        github.event.comment.author_association == 'MEMBER' ||
        github.event.comment.author_association == 'COLLABORATOR' ||
        github.event.review.author_association == 'OWNER' ||
        github.event.review.author_association == 'MEMBER' ||
        github.event.review.author_association == 'COLLABORATOR' ||
        (github.event.pull_request && github.event.pull_request.user.login == github.event.comment.user.login) ||
        (github.event.pull_request && github.event.pull_request.user.login == github.event.review.user.login) ||
        (github.event.issue && github.event.issue.user.login == github.event.comment.user.login)
      ) && (
        (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude'))
        || (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude'))
        || (github.event_name == 'pull_request_review'
          && github.event.review.body != null
          && github.event.review.body != ''
          && contains(github.event.review.body, '@claude'))
        || (github.event_name == 'issues'
          && github.event.action == 'opened'
          && (
            github.event.issue.author_association == 'OWNER'
            || github.event.issue.author_association == 'MEMBER'
            || github.event.issue.author_association == 'COLLABORATOR'
          )
          && (contains(github.event.issue.body, '@claude')
            || contains(github.event.issue.title, '@claude')))
      )
    name: Claude Code
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required for Claude to push code changes
      pull-requests: write
      issues: write
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    env:
      # Allowed tools for Claude (comma-separated list)
      # Add new tools here as needed
      ALLOWED_TOOLS: >-
        Bash(git:*),
        Bash(gh issue:*),
        Bash(gh pr:*),
        Bash(gh search:*),
        Bash(pre-commit run:*),
        Bash(jq:*),
        Bash(yq:*),
        Bash(npm run lint:*),
        Bash(npm install)
    steps:
      - name: Debug workflow trigger
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_ACTION: ${{ github.event.action }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          COMMENT_AUTHOR: ${{ github.event.comment.user.login }}
          COMMENT_AUTHOR_ASSOC: ${{ github.event.comment.author_association }}
          REVIEW_BODY: ${{ github.event.review.body }}
          REVIEW_AUTHOR: ${{ github.event.review.user.login }}
          REVIEW_AUTHOR_ASSOC: ${{ github.event.review.author_association }}
        run: |
          echo "Event name: $EVENT_NAME"
          echo "Action: $EVENT_ACTION"
          echo "Comment body: $COMMENT_BODY"
          echo "Comment author: $COMMENT_AUTHOR"
          echo "Comment author_association: $COMMENT_AUTHOR_ASSOC"
          echo "Review body: $REVIEW_BODY"
          echo "Review author: $REVIEW_AUTHOR"
          echo "Review author_association: $REVIEW_AUTHOR_ASSOC"

      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          additional_permissions: |
            actions: read
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_args: |
            --model claude-opus-4-5
            --max-turns 100
            --allowedTools "${{ env.ALLOWED_TOOLS }}"
          github_token: ${{ secrets.GITHUB_TOKEN }}

          # Models: <https://platform.claude.com/docs/en/about-claude/models/overview>

          # Optional: Customize the trigger phrase (default: @claude)
          # trigger_phrase: "/claude"


          # Optional: Configure Claude's behavior with CLI arguments
          # claude_args: |
          #   --model claude-opus-4-1-20250805
          #   --allowedTools "Bash(npm install),Bash(npm run build),Bash(npm run test:*),Bash(npm run lint:*)"
          #   --system-prompt "Follow our coding standards. Ensure all new code has tests."

          # Optional: Advanced settings configuration
          # settings: |
          #   {
          #     "env": {
          #       "NODE_ENV": "test"
          #     }
          #   }

---
name: git
description: Guide for using git.
---

## Dont's:

- Avoid triggering interactive editor when running `git` (e.g. `--no-edit`).
- Avoid triggering git hooks during rebase, use `--no-verify` instead.

## Useful commands:

- `git config user.name && git config user.email`
